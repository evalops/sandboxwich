kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
networking:
  disableDefaultCNI: true
  # Dual-stack so the FQDN conformance suite can prove IPv6 allow and deny
  # against real AAAA records instead of asserting IPv4-only behavior.
  ipFamily: dual
nodes:
  - role: control-plane
  - role: worker
